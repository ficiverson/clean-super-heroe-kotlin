package org.buffer.android.boilerplate.domain.model

/**
 * Representation for a [SuperHeroe] fetched from an external layer data source
 */
data class SuperHeroe(val name: String, val title: String, val avatar: String)